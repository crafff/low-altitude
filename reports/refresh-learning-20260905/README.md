# Current-state-refresh PPO development learning curve

This figure contains thirteen actual evaluations at0/25/.../300 episodes from one fresh current-state-refresh lineage and the same12 predeclared development cases. Old cached-guidance400 rounds are not included or added. NR and sample share exact scenarios and execution definitions. No held-out observations, independent training replicates, smoothing, uncertainty intervals or significance claims are shown.

Sample completion starts246/360, reaches a maximum259 at200, and ends252 at300; the corresponding lateral-outside flights are339/339/335. Episode300 NMAC is74.440 unordered pair-seconds per flight-hour versus initial78.926; the trajectory fluctuates and remains far from reliable completion/containment. Best200 is not an effective baseline. See the separate [initial/100 sample–argmax diagnostic](../policy-modes-refresh-100-20260905/README.md); sample remains primary. The [passive100 lateral audit](../lateral-exposure-refresh-100-20260905/README.md) replays that saved round exactly and identifies execution-state/type exposure without altering the curve.

`input.json` retains each aggregate's exact numerators/denominators and source file SHA/line. The renderer validates rates and population accounting. The duplicated100 aggregates at the recovery boundary were exactly equal before merging. Final render: `runs/20260905T121247Z-refresh-learning-figure-300-0254bc91`, 3.296 supervisor seconds; all SVG/PNG/PDF/metadata files were byte-verified after copying, and the PNG was visually checked. SVG/PNG/metadata are tracked; PDF remains local under the repository exclusion rule.

The earlier render `runs/20260905T111403Z-refresh-learning-figure-100-af4a767e` retained correct plotted values but had the last subplot title as its overall title because a loop variable shadowed the new optional figure title. That run remains intact. The renderer now uses a separate figure-title variable; the focused actual-export regression covers default and custom titles. Its first fixture incorrectly assumed path-font SVG comments, while this renderer exports text nodes; the corrected XML-text fixture passed in `runs/20260905T111652Z-learning-title-regression-svg-text-a1fa873b`.

Reproduce through `tools/lab.py run`, explicitly passing `reports/refresh-learning-20260905/input.json` as input, then project Python `-B -m learning_curve_plot --input reports/refresh-learning-20260905/input.json`. Use outer45s/32MiB/2048MiB, project Python/managed runtime, CPU14/nice15/idle IO and one launcher. The input supplies the lineage title; omitted titles preserve the existing cached-guidance default. Metadata includes the renderer source hash.
