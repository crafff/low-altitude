# Current-state-refresh PPO development learning curve

This figure contains the five actual evaluations at0/25/50/75/100 episodes from one fresh current-state-refresh training lineage, using the same 12 predeclared development scenarios. It starts from the original initialization; old cached-guidance400 rounds are not included or added. NR and sample share each exact scenario and execution definition. No held-out observations, independent training replicates, smoothing, uncertainty intervals or significance claims are shown.

Sample completion is246/240/252/251/240 out of360, while339/334/336/337/341 flights record lateral outside time. NMAC exposure is78.926/88.123/76.568/84.148/78.075 unordered pair-seconds per flight-hour. The better episode50 point has not become a consistent trend; there is no effective baseline or containment claim. See the separate [initial/100 sample–argmax diagnostic](../policy-modes-refresh-100-20260905/README.md) for the measured deployment difference. Sample remains primary.

`input.json` retains each aggregate's exact numerators/denominators and source file SHA/line. The renderer validates rates and population accounting. Final render: `runs/20260905T111615Z-refresh-learning-figure-100-title-fixed-0e6283ec`, 3.343 supervisor seconds; all SVG/PNG/PDF/metadata files were byte-verified after copying, and the PNG was visually checked. SVG/PNG/metadata are tracked; PDF remains local under the repository exclusion rule.

The earlier render `runs/20260905T111403Z-refresh-learning-figure-100-af4a767e` retained correct plotted values but had the last subplot title as its overall title because a loop variable shadowed the new optional figure title. That run remains intact. The renderer now uses a separate figure-title variable; the focused actual-export regression covers default and custom titles. Its first fixture incorrectly assumed path-font SVG comments, while this renderer exports text nodes; the corrected XML-text fixture passed in `runs/20260905T111652Z-learning-title-regression-svg-text-a1fa873b`.

Reproduce through `tools/lab.py run`, explicitly passing `reports/refresh-learning-20260905/input.json` as input, then project Python `-B -m learning_curve_plot --input reports/refresh-learning-20260905/input.json`. Use outer45s/32MiB/2048MiB, project Python/managed runtime, CPU14/nice15/idle IO and one launcher. The input supplies the lineage title; omitted titles preserve the existing cached-guidance default. Metadata includes the renderer source hash.
