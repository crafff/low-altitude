# Native validation of ordinary-flyby current-state refresh

The new runtime adapter passed its native integration comparisons in `runs/20260905T103332Z-navigation-refresh-native-09ae0b8a` (204.281 internal seconds, 205.973 under the supervisor). This validates the declared execution correction; it does not establish an effective policy or corridor containment.

- Default `native_cached`: all 12 original development cases under NR and the exact seed-61001 untrained sampled policy reproduce every saved scientific per-case field and aggregate. Only wall time and RSS are excluded. No refresh wrapper was installed.
- Explicit `current_state_refresh`: four fixed north-Amzn responses reproduce the audited external-refresh summaries, phase/milestone packets and all 2382 parsed physical CSV rows. Only the intentional diagnostic arm label differs. Actual wrapper/native calls are 761, 775, 430 and 412; all step contexts exit and restore the original method.
- Full 30-aircraft seed53001: integrated and external refresh agree exactly under NR and sample, including all individual-flight records, physical-state stream hashes and independently observed native-call streams. NR makes 5451 calls, with active populations 1–19; sample makes 5965, with populations 1–20. Each has 56 observed population-ID changes. Resets start with empty audit counters; methods/callbacks restore after each run. Native receives one call per wrapper invocation. Zero-population native updates may be skipped and are not inferred from total simulation time.
- Both modes use unchanged model tensors and no gradients, optimizer, checkpoint loading, training or selection. All eight result checks pass.

The sampled 30-aircraft case still completes only 22 flights, with three timeouts, five navigation-exhaustion failures and 21 flights with recorded lateral outside time (4410.25 aircraft-s). NR completes 30/30 without lateral outside time in this case. These are one-case outputs, not evidence that all development cases are feasible or the policy is effective.

Controller tests: `runs/20260905T102203Z-navigation-refresh-core-tests-feb50c3b` passed 19 relevant adapter/environment/training regressions; `runs/20260905T103310Z-navigation-refresh-probe-tests-aee50278` passed three comparison regressions. Independent read-only review found no blocker to native validation. The prior [fixed-response/source investigation](../route-response-20260905/README.md) explains the correction and terminal-step accounting limitation.

`result.json` is the exact native result, with effective configurations, source/input hashes, individual outcomes and audits. Raw CSV/event files remain in the run. Reproduce through `tools/lab.py run` with `configs/navigation_refresh_probe.json`, module `navigation_refresh_probe`, and explicit inputs/CLI arguments for the original zero-episode development JSONL, route-response result and selected four-case physics CSV. Use outer420s/128MiB/4096MiB, the project Python/managed runtime, CPU14/nice15/idle IO and one launcher job.

The new scientific source identity includes `navigation_refresh.py`; the environment option is explicit in `paper_environment_execution_refresh.json` and selected by `paper_train_execution_refresh.json`. Old 400-episode checkpoints remain in their compatible cached-guidance lineage. Fresh training retains the original initialization, scenario/action/shuffle streams, all 12 development cases, Table 3, bank, corridor dimensions, actions, observation/reward and termination accounting.
