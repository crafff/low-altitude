# 关键决定

## 2026-09-04/05：停止深度恢复并从头搭建系统

- 用户接受将已恢复资料归档到同分区legacy；保留缺失清单和负结果，未恢复部分不冒充完整。
- 只重建工作方式，不丢失论文目标、有用代码和教训。旧baseline半成品不直接复活。
- 不再使用Claim/Experiment/Evidence控制链。一个任务可连续DEV迭代，每次执行自动保存记录；独立评估不是每个小步骤都要单建审批对象。
- 子agent统一Astra/xhigh；默认模型与角色配置显式固定，当前线程仍以实际元数据为准。
- 首项科学任务仍是无延迟有效基线，然后固定模型加入执行延迟。不借系统重建宣称实验已有进展。
- 运行器必须OS隔离且保留固定源码快照；缺失隔离不回退裸执行。磁盘/内存边界按真实实现说明，独立备份仍未完成。

## 2026-09-05：先隔离执行语义差异，再扩大PPO

literal环境两轮native学习已接通，但原生转弯越界与末端横移后导航结束已由定向轨迹证实。保留82fd7cf源码及恢复点，不通过增加训练掩盖物理执行缺陷。公开2024论文/参数/作者fork仍不足以确定2026导航细节；论文Table3数值暂不被旧fork参数替换。

分别诊断三种明确选择：原25m端点的连续段判定；固定走廊末端有限宽高截面的正向穿越；最后航段不接受会把capture点放到出口之后的新横移。前者保持端点定义，第二项改变完成任务定义，第三项改变动作可用性；都不是作者确认设置，也不是我们的预测/数学安全过滤器。native导航耗尽却未成功必须单列失败，不能计到达奖励；较短失败尾段不构成安全改善。高度机动追加VS稳定条件只作为待实测的完成语义候选，是否确实减少越界需同链路证据。

先看几何、目标执行和运输完成，再看安全率。所有配置与NR使用同一终止/动作执行链，当前仍无可冻结的有效完整baseline。

## 2026-09-05 07:02 UTC：有限出口候选进入25轮开发训练

选择paper_environment_execution的有限宽高出口、VS稳定完成及最后capture guard作独立execution诊断；保留literal与swept配置。理由是任务退出同时要求穿过实际走廊的出口，不能因25m邻域会接纳边界外末端而只按较高完成率选它。finite_exit random完成19/30、swept21/30的差异已经记录，这不是训练前的性能收益选择。NR保持30/30，失败仍明确终止且不计到达奖励。

Table3全部速度与原生bank不变。旧44m/s的Amzn中心弯道最大偏离降到76.028m，但内侧仍150.028m；普通flyby刷新未消除Table3速度的转弯越界，因此两种来源敏感性都不加入主训练。横向管道约束尚不满足，当前训练只检验在明确执行语义下能否产生有效学习趋势，不认定完整paper baseline。

先训练总25轮、内600s/外660s，使用原先声明的全部12个开发场景作初始/末次评价，不打开最终测试。以完成/失败/暴露/越界共同解释结果；文件名best不等于有效模型。随后根据实际吞吐、学习趋势和剩余墙钟决定续训，源身份不匹配时不强行恢复。
